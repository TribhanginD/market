# CODEX.md

Use `CLAUDE.md` as main repo guide.

## Codex Extra

- Read `CLAUDE.md` first.
- Respect `.codexignore`.
- Prefer targeted edits. Repo has large generated `storage/` trees.
- When changing data shape, grep dashboard + persistence + tests before patch.
- Do not clean `storage/` beyond transient junk unless task says so.
- Preserve adversarial behavior. Do not simplify debate into one-agent summary flow.
- If touching Stage 2 or Stage 3, optimize for disagreement quality, not more prose.
